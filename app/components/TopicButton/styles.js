import { StyleSheet } from 'react-native';
import colors from '../../config/colors';

export default StyleSheet.create({
TopicButtonText: {
    textAlign: 'center',
    paddingVertical: 15,
    color: '#fff',
    fontSize: 14,
    fontWeight: '500',
  },
});
